import { Grid } from './Grid';

// eslint-disable-next-line import/prefer-default-export
export declare function startResize(
  grid: Grid,
  column: string,
  ev: MouseEvent & TouchEvent,
  cb: never
): void;
